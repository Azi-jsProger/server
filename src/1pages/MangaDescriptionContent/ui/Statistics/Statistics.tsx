import React from 'react';
import cls from "Descriptions.module.scss";

type TDescriptions = {

}
const Descriptions = (props: TDescriptions) => {
    const {  } = props

    return (
        <div>
            
        </div>
    );
};

export default Descriptions;